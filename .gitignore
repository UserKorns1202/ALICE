# Secrets / credentials — never commit these
alice_secrets.py
.env
*.env

# Google OAuth tokens and service-account credentials
token.json
credentials.json
creds.json

# VAPID / push-notification keys
vapid_keys.json

# Remote access token
remote_token.txt

# Python bytecode and caches
__pycache__/
*.py[cod]
*.pyo

# Virtual environment
.venv/

# IDE settings
.vscode/

# OS files
.DS_Store
Thumbs.db

# OCR tmp: ignore all files under the OCR tmp folder but keep the folder tracked via .gitkeep
/modules/ocr_service/tmp/*
!/modules/ocr_service/tmp/.gitkeep

piper\log.txt
piper\piper_stdout.log
user_memory.json
vrgl_bot.log
vrgl_config.json
helldivers_state.json
context.db